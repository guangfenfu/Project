

<h1>Users</h1>


<table border="1">
  <tr>
    <th>Name</th>
    <th>Role</th>
  </tr>

<% @users.each do |user| %>
  <tr>
    <td><%= link_to user.name, :action => 'show', :id => user.id %></td>
    <td><%= user.role.name %></td>
  </tr>
<% end %>

</table>
